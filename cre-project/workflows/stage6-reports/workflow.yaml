# ============================================================
# Stage 6 – Regulatory Report Workflow (StealthBond LATAM)
# ============================================================
name: "stage6-reports"
type: "typescript"

simulation:
  user-workflow:
    workflow-name: "stage6-reports"
  workflow-artifacts:
    workflow-path: "./index.ts"
    config-path: "./config.json"
    secrets-path: "../../secrets.yaml"

capabilities:
  - "http"
